import React, { PureComponent } from 'react';
import AppLayout from '../../shared/AppLayout';

export class Delete extends PureComponent {
  render() {
    return <AppLayout>Delete Page</AppLayout>;
  }
}

export default Delete;
